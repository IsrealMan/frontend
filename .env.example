# ── Frontend environment template ────────────────────────────
# Copy this file to .env for local development.
# .env.production is committed separately and holds the production URL.
#
# All variables MUST be prefixed with VITE_ to be exposed to the browser.

# Backend API base URL.
#   Dev:  Leave empty — vite.config.js proxy forwards /api and /auth
#         to http://localhost:3000.
#   Prod: Set to your deployed backend URL, e.g.
#         https://backend-5tvm.onrender.com
VITE_API_URL=
